<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TrackMyMobile" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TrackMyMobile" parent="Base.Theme.TrackMyMobile" />
    <style name="CommonButtonPrimary" parent="TextAppearance.AppCompat.Button">
        <item name="android:textSize">@dimen/size_18sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:layout_marginRight">@dimen/size_12dp</item>
        <item name="android:layout_marginLeft">@dimen/size_12dp</item>
        <item name="background">@drawable/background_button_primary</item>
        <item name="android:gravity">center</item>
        <item name="android:fontFamily">@font/poppins_bold</item>
        <item name="textAllCaps">false</item>
    </style>

</resources>